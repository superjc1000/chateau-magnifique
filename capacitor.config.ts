import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'chateau magnifique',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
